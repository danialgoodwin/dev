# Simple To-Do List Project in Ruby #

This To-Do list uses only the Ruby basics, and convensions.

Client interaction is only done with `TodoList`. There are some sample usage code in `todo_list.rb` at the bottom.
